// Typing Indicator Component\n// Shows when users are typing with animated dots\n\nimport React from 'react';\nimport { cn } from '../../utils/cn';\n\nexport interface TypingIndicatorProps {\n  users: string[];\n  className?: string;\n}\n\nconst TypingIndicator: React.FC<TypingIndicatorProps> = ({\n  users,\n  className\n}) => {\n  if (users.length === 0) {\n    return null;\n  }\n\n  const getTypingText = () => {\n    if (users.length === 1) {\n      return `${users[0]} กำลังพิมพ์...`;\n    } else if (users.length === 2) {\n      return `${users[0]} และ ${users[1]} กำลังพิมพ์...`;\n    } else {\n      return `${users[0]} และอีก ${users.length - 1} คน กำลังพิมพ์...`;\n    }\n  };\n\n  return (\n    <div className={cn('flex items-center gap-3', className)}>\n      {/* Avatar placeholder */}\n      <div className=\"flex-shrink-0\">\n        <div className=\"w-8 h-8 bg-gray-200 rounded-full flex items-center justify-center\">\n          <svg className=\"w-5 h-5 text-gray-600\" fill=\"none\" stroke=\"currentColor\" viewBox=\"0 0 24 24\">\n            <path strokeLinecap=\"round\" strokeLinejoin=\"round\" strokeWidth={2} d=\"M16 7a4 4 0 11-8 0 4 4 0 018 0zM12 14a7 7 0 00-7 7h14a7 7 0 00-7-7z\" />\n          </svg>\n        </div>\n      </div>\n\n      {/* Typing bubble */}\n      <div className=\"flex-1\">\n        <div className=\"bg-gray-100 rounded-2xl rounded-bl-md px-4 py-2 max-w-xs\">\n          <div className=\"flex items-center gap-2\">\n            {/* Typing text */}\n            <span className=\"text-sm text-gray-600\">\n              {getTypingText()}\n            </span>\n            \n            {/* Animated dots */}\n            <div className=\"flex items-center gap-1\">\n              <div className=\"w-1.5 h-1.5 bg-gray-400 rounded-full animate-bounce\" style={{ animationDelay: '0ms' }} />\n              <div className=\"w-1.5 h-1.5 bg-gray-400 rounded-full animate-bounce\" style={{ animationDelay: '150ms' }} />\n              <div className=\"w-1.5 h-1.5 bg-gray-400 rounded-full animate-bounce\" style={{ animationDelay: '300ms' }} />\n            </div>\n          </div>\n        </div>\n      </div>\n    </div>\n  );\n};\n\n// Alternative minimal typing indicator\nexport const MinimalTypingIndicator: React.FC<TypingIndicatorProps> = ({\n  users,\n  className\n}) => {\n  if (users.length === 0) {\n    return null;\n  }\n\n  return (\n    <div className={cn('flex items-center gap-2 text-sm text-gray-500', className)}>\n      <div className=\"flex items-center gap-1\">\n        <div className=\"w-1 h-1 bg-gray-400 rounded-full animate-pulse\" />\n        <div className=\"w-1 h-1 bg-gray-400 rounded-full animate-pulse\" style={{ animationDelay: '200ms' }} />\n        <div className=\"w-1 h-1 bg-gray-400 rounded-full animate-pulse\" style={{ animationDelay: '400ms' }} />\n      </div>\n      <span>\n        {users.length === 1 ? `${users[0]} กำลังพิมพ์` : `${users.length} คน กำลังพิมพ์`}\n      </span>\n    </div>\n  );\n};\n\nexport { TypingIndicator };\n